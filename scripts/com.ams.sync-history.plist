<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Unique identifier for this agent -->
  <key>Label</key>
  <string>com.ams.sync-history</string>

  <!-- Run the sync script via bash (launchd ignores shebangs) -->
  <key>ProgramArguments</key>
  <array>
    <string>/bin/bash</string>
    <string>-l</string>
    <string>/Users/adrianwalderhaug-johnsen/Documents/athlete-management-system/scripts/sync_history.sh</string>
  </array>

  <!-- Once a night, well clear of the sync-kpis 6:30 AM calendar trigger. This is
       the heavier full backfill (up to 365 days) — not throttled internally like
       sync-kpis, so it only needs one guaranteed firing a day, not a frequent
       trigger-opportunity interval. If the Mac is asleep at 3:15, launchd runs it
       on wake instead. -->
  <key>StartCalendarInterval</key>
  <dict>
    <key>Hour</key>
    <integer>3</integer>
    <key>Minute</key>
    <integer>15</integer>
  </dict>

  <!-- Write stdout/stderr to log files for debugging -->
  <key>StandardOutPath</key>
  <string>/Users/adrianwalderhaug-johnsen/.ams-history-sync.log</string>
  <key>StandardErrorPath</key>
  <string>/Users/adrianwalderhaug-johnsen/.ams-history-sync.log</string>

  <!-- Keep environment clean -->
  <key>EnvironmentVariables</key>
  <dict>
    <key>PATH</key>
    <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
    <key>HOME</key>
    <string>/Users/adrianwalderhaug-johnsen</string>
  </dict>
</dict>
</plist>
